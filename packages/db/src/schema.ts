/**
 * Drizzle schema.
 *
 * Milestone 1 only provisions the database foundation (pgvector extension).
 * Domain tables (documents, document_versions, chunks, ingestion_jobs) are
 * introduced by their respective milestones and defined here.
 */
export {};
